# ============================================================================
# JUEZ SEGURO - Variables de Entorno para Docker Compose
# ============================================================================
# Copia este archivo como .env y configura los valores reales
# ============================================================================

# ============================================================================
# ENTORNO DE EJECUCIÓN
# ============================================================================
# development: Habilita scripts automáticos de seed (usuarios y secretos)
# production: Deshabilita scripts automáticos (más seguro)
# IMPORTANTE: Usar 'production' en servidores públicos
NODE_ENV=production

# ============================================================================
# CONTRASEÑAS DE BASES DE DATOS
# ============================================================================
# PRODUCCIÓN: Generar con: openssl rand -base64 32
# DESARROLLO: Puedes usar contraseñas simples pero documéntalas
DB_PASS_USERS=<contraseña-segura-32-chars>
DB_PASS_CASES=<contraseña-segura-32-chars>
DB_PASS_LOGS=<contraseña-segura-32-chars>
DB_PASS_SECRETS=<contraseña-segura-32-chars>

# ============================================================================
# CLAVE MAESTRA (FCS - Cryptographic Support)
# ============================================================================
# Esta es la ÚNICA clave que necesitas configurar externamente.
# Todos los demás secretos se almacenan encriptados en db_secrets.
# IMPORTANTE: Usar al menos 48 caracteres con alta entropía
# PRODUCCIÓN: Generar con: openssl rand -base64 48
# ============================================================================
MASTER_KEY_PASSWORD=<clave-maestra-muy-segura-64-chars>

# ============================================================================
# CONFIGURACIÓN JWT
# ============================================================================
# El secreto JWT real se almacena encriptado en db_secrets
# Esta variable solo configura el tiempo de expiración
JWT_EXPIRATION=8h

# Nota: Las variables de entorno del backend y frontend
# están en sus respectivos directorios:
# - backend/.env
# - frontend/.env
